- [x] `spec.md` 已明确本次变更只针对 `AGENTS.md` 的 Mermaid 可视化升级，不引入无关范围扩张。
- [x] `tasks.md` 已覆盖内容盘点、规范补充、图表替换与兼容性校验四类核心工作。
- [x] 规范中已明确要求后续维护者对流程、架构、关系、职责、层级与目录流转等可视化内容优先使用 Mermaid。
- [x] 规范中已明确允许保留必要的精炼文字说明，但不得以纯文字重复图表已表达的结构信息。
- [x] 规范中已要求对现有纯文字流程、关系与分层内容进行 Mermaid 图表化替换。
- [x] 规范中已包含对 `flowchart`、`classDiagram`、`sequenceDiagram` 与兼容 Markdown 的 C4 风格表达的适配要求。
- [x] 规范中已要求新增图表在 GitHub、GitLab、Notion 等主流 Markdown 编辑环境下保持语法兼容性。
- [x] 规范中已明确更新后必须保留原有核心业务内容与语义边界，仅优化呈现形式。
- [x] 规范中已要求更新后的 `AGENTS.md` 保持逻辑连贯、视觉清晰并符合专业技术文档写作标准。
