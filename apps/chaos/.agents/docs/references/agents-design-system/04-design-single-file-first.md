# 4. 设计方向一：单文件优先，目录增强

现在比较健康的设计是：

```
AGENTS.md              # 必须存在
.agents/               # 可选，复杂项目再引入
```

而不是只用 `.agents/`。原因是很多工具默认会优先查找根目录的指令文件。单文件入口更容易被工具发现，也更容易被人类理解。

**AGENTS.md 负责：** 简明项目概览、Agent 总规则、常用命令、文件边界、工作流要求、链接到 `.agents/` 里的细节。

**.agents/ 负责：** 长文档、特定角色、特定任务、深层架构说明、复杂 policy。
