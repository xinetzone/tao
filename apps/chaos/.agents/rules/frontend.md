---
paths:
  - "src/**/*.{tsx,jsx,ts,js,css,scss}"
  - "apps/web/**/*"
---

# 前端开发规范 (Frontend Guidelines)

> **注意**：这是一份模板文件，请根据实际项目技术栈进行修改。

## 核心技术栈
- **框架**: (待定义，例如 React / Vue / Next.js)
- **样式**: (待定义，例如 Tailwind CSS / CSS Modules)
- **状态管理**: (待定义)

## 规范要求
1. **单一职责**：组件必须保持单一职责，复杂的组件需拆分为子组件。
2. **语义化**：优先使用语义化 HTML 标签。
3. **响应式**：必须考虑移动端优先 (Mobile First) 的响应式设计。
4. **代码风格**：(可以在此指定如 ESLint/Prettier 的特定规则要求)
