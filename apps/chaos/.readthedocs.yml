version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
    nodejs: "22"

sphinx:
  configuration: docs/conf.py

formats:
  - htmlzip

python:
  install:
    - method: uv
      command: sync
      groups:
        - dev
        - docs
