- [x] 项目根目录已定义符合 `mise` 规范的配置文件，并集中声明开发所需工具及精确版本。
- [x] `mise` 配置覆盖至少 Python、`uv`、Node.js、`pre-commit`、`ruff`、文档构建相关命令入口及项目依赖的外部 CLI。
- [x] Python 工具与项目依赖通过 `uv` 直接执行或同步，初始化脚本、文档与 CI/CD 不使用 `pipx`。
- [x] 环境初始化脚本可在 PowerShell 7 中一键完成 `mise` trust、工具安装、`uv` 直接依赖同步、环境变量准备与首次校验。
- [x] 初始化脚本在失败时能明确提示缺失工具、失败步骤和推荐修复命令。
- [x] 人类文档已补充 `mise` 本体安装指引、项目初始化命令、版本更新流程与常见问题排查方案。
- [x] `README.md` 与 `docs/` 中关于环境准备和命令执行的说明已切换为 `mise` 优先入口。
- [x] `ci.yml`、`pages.yml`、`python-publish.yml` 与 `release.yml` 已适配 `mise` 环境安装流程。
- [x] CI/CD 在执行测试、Lint、文档构建或发布前，会先使用 `mise` 安装并激活项目声明的工具链。
- [x] 环境一致性校验脚本能够输出关键工具的期望版本、当前版本与推荐修复命令。
- [x] 本地初始化链路与 CI/CD 流水线共享同一份工具版本声明，不再维护重复的版本硬编码来源。
